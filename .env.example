# Postgres / docker-compose
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=app
POSTGRES_PORT=5432

# FastAPI (runs locally, connects to the containerized Postgres)
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/app
